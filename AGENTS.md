# Агенты

ТЗ, фича, баг — сначала skill `.cursor/skills/tz-pipeline/SKILL.md`.

Роли: `analyst`, `architect`, `lead`, `developer`, `tester` в `.cursor/agents/`.

Не менять HTTP-ручки и OpenAPI без разрешения. Не писать комментарии в исходниках. После зелёных проверок без замечаний — влить в `origin/main`. После ТЗ — отчёт lead (сделано / не сделано / проблемы / main).
